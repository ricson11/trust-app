<div class="container my-4">
    <form action="/email" method="POST">
    <h5 class="text-center my-4">Email {{user.username}}</h5>
    <div class="form-group">
        <input type="email" class="form-control" name="mail" value="{{ask.email}}" id="ema">
    </div>
      <div class="form-group">
        <input type="email" class="form-control " name="mail2" value="{{user.email}}" id="ema2">
    </div>
    <div class="form-group">
        <textarea name="message" class="form-control" rows="5" id="ema3" placeholder="hi! {{user.username}}, {{ask.username}} want to email you"></textarea>
    </div>
    <div class="form-group">
        <button class="btn btn-danger" type="submit">Send</button>
    </div>
    </form>
</div>


<style>
    .form-control{
        background: ghostwhite;
    }
    #ema2{
        color:ghostwhite;
    }
</style>


<script>

   var emabtn = document.querySelector('#ema') ;
    var emabtn2 = document.querySelector('#ema2') ;
   var emabtn3 = document.querySelector('#ema3') ;



   emabtn.addEventListener('mousedown', function(e){
     e.preventDefault();
   })
   
   emabtn2.addEventListener('mousedown', function(e){
     e.preventDefault();
   })
   
   emabtn3.addEventListener('mousedown', function(e){
     e.preventDefault();
   })
</script>